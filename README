My VIM settings.  Lots of thanks to @mgrouchy and mirnazim for their repo's.

Two externals right now:

pip install -e git+https://github.com/kevinw/pyflakes.git#egg=pyflakes
apt-get install exuberant-ctags

git clone git@github.com:volksman/dotvim.git
cd dotvim

git submodule init
git submodule update

cd ~
rm -rf .vim
ln -s dotvim/.vim
ln -s dotvim/.vimrc
